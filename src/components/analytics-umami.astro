{
  import.meta.env.PROD && Astro.url.hostname !== "localhost" && (
    <script
      is:inline
      defer
      src="https://cloud.umami.is/script.js"
      data-astro-rerun
      data-website-id={import.meta.env.UMAMI_WEBSITE_ID}
    />
  )
}
