---
import CardProfile from "./card-profile.svelte";

const { login, session } = Astro.locals;
const name = session?.user?.name ?? "Local data";
const image = session?.user?.image ?? undefined;
---

<CardProfile client:load {name} {image} {login} />
